import { CitizenPortal } from "@/components/citizen/CitizenPortal";

/**
 * /report — the public citizen grievance portal. Mobile-first, no MP shell.
 */
export default function CitizenReportPage() {
  return <CitizenPortal />;
}
