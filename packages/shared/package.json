{
  "name": "@saarthi/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Saarthi shared domain model — the single source of truth for Firestore + BigQuery shapes.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "echo \"(no tests yet)\" && exit 0",
    "lint": "echo \"(no lint yet)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
