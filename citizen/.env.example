# Saarthi Citizen Portal — environment
# Copy to .env.local. Points the portal at the shared Saarthi tickets API.
# In production this is the MP dashboard / API deployment's URL.
NEXT_PUBLIC_SAARTHI_API_URL=http://localhost:3000
